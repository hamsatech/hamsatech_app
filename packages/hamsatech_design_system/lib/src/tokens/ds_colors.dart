import 'package:flutter/material.dart';

class DSColors {
  DSColors._();

  // ── Brand — Teal (logo circle) ────────────────────────────────────────────
  static const Color brand        = Color(0xFF14B8A6);
  static const Color brandHover   = Color(0xFF0D9488);
  static const Color brandPressed = Color(0xFF0F766E);
  static const Color brandMuted   = Color(0x1A14B8A6);

  // ── Accent — Terracotta (warm app brand) ─────────────────────────────────
  static const Color terracotta        = Color(0xFFC94B2A);
  static const Color terracottaHover   = Color(0xFFB14224);
  static const Color terracottaPressed = Color(0xFF9B3A1F);
  static const Color terracottaMuted   = Color(0x1AC94B2A);

  // ── Accent — Coral (logo heart) ───────────────────────────────────────────
  static const Color coral        = Color(0xFFF87171);
  static const Color coralMuted   = Color(0x1AF87171);

  // ── Accent — Navy (logo hands) ────────────────────────────────────────────
  static const Color navy         = Color(0xFF0F2A47);
  static const Color navyLight    = Color(0xFF1A3D60);

  // ── Neutrals ─────────────────────────────────────────────────────────────
  static const Color black = Color(0xFF09090B);
  static const Color white = Color(0xFFFFFFFF);

  static const Color gray50  = Color(0xFFF0F9FF);
  static const Color gray100 = Color(0xFFE0F2FE);
  static const Color gray200 = Color(0xFFBAE6FD);
  static const Color gray300 = Color(0xFF7DD3FC);
  static const Color gray400 = Color(0xFF38BDF8);
  static const Color gray500 = Color(0xFF0EA5E9);
  static const Color gray600 = Color(0xFF0284C7);
  static const Color gray700 = Color(0xFF0369A1);
  static const Color gray800 = Color(0xFF075985);
  static const Color gray900 = Color(0xFF0C4A6E);
  static const Color gray950 = Color(0xFF082F49);

  // ── Semantic ──────────────────────────────────────────────────────────────
  static const Color error        = Color(0xFFEF4444);
  static const Color errorSurface = Color(0xFF1A0A0A);
  static const Color warning      = Color(0xFFF59E0B);
  static const Color success      = Color(0xFF10B981);
  static const Color successLight = Color(0xFF34D399);
  static const Color info         = Color(0xFF38BDF8);
  static const Color infoLight    = Color(0xFF7DD3FC);

  // ── App surface colours (dark teal-navy theme) ────────────────────────────
  static const Color appBackground   = Color(0xFF040C18);
  static const Color appSurface      = Color(0xFF081525);
  static const Color appCard         = Color(0xFF0D1E35);
  static const Color appCardElevated = Color(0xFF122440);
  static const Color appBorder       = Color(0xFF1E3A55);
  static const Color appDivider      = Color(0xFF0F1F30);

  // ── App text colours ──────────────────────────────────────────────────────
  static const Color textPrimary   = Color(0xFFF0F9FF);
  static const Color textSecondary = Color(0xFF7ECFD4);
  static const Color textMuted     = Color(0xFF3B6070);
}
