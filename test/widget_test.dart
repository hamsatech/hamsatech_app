import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Full integration tests require storage initialisation.
    // Unit tests for BLoCs should be added per feature.
    expect(true, isTrue);
  });
}
